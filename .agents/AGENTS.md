# Workspace Rules

- As a matter of rule, prefer file-level `use` statements over scoped/function-level `use` statements or inline full-paths (e.g. `std::fmt::Display`). All imports should be grouped at the top of the file.
